{
  "name": "orrery",
  "version": "1.0.0",
  "private": true,
  "description": "An interactive N-body gravity sandbox in a single canvas",
  "scripts": {
    "test": "node --test",
    "build": "npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
